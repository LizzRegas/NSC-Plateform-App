{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter @workspace/church-app run dev",
    "build": "pnpm --filter @workspace/church-app run build",
    "typecheck": "pnpm --filter @workspace/church-app run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
